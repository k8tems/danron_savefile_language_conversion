I have a python script that I'd like you to wrap as a webapp with a simple user interface.

Script details:
The script converts the language of a certain game's savefile while fixing the checksum.
Use convert.py as reference for the core functionality, but you can modify or rewrite the code if deemed necessary.

Design details:
Make it a 90s style website titled セーブファイル言語変換
Use header.png as the header image(fully colored;2720x1536)

Webapp details:
Use fastapi and python for the framework, but use vanilla CSS/JS.
The user will upload a savefile, after which the app will display the current text and voice language of the file.
Then the user will modify the displayed attributes and tell the website toe run the conversion.
After the conversion is done, either a button for downloading will be displayed for clicking, or the download will automatically start.
I will leave that decision up to you.
The style should be retro, but follow modern frontend practices to keep the website responsive.
If the user accesses the website from mobile, tell them the website is intended for pc use, and display sayaka.png, which is an image of a character from the game making a shocked gesture.(transparent background with character in center; 500x500)
