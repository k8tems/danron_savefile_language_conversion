I don't like how trailing bytes are passed around when valid files will always end with 0xDEADBEEF.
Before we start implementing the refactor, I'd like you to add a failing test that checks that the trailing DWORD is 0xDEADBEEF in savedata_jp_no_deadbeef.vfs.
If they are not, raise an exception in the parsing function.
Also add a test for savedata_jp.vfs which should not fail due to 0xDEADBEEF being there.
When you verify that the tests both fail, start implementing the refactor.
